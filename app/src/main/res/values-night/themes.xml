<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SantanderDevWeek" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/secondaryColor</item>
        <item name="colorPrimaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnPrimary">@color/textColor</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/primaryColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Estilo para alterar cor estilo botões-->
    <style name="button_style" parent="@style/Widget.MaterialComponents.Button.OutlinedButton.Icon">
        <item name="android:textColor">?attr/colorControlNormal</item>
        <item name="backgroundTint">@color/bgButtonColorNigth</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <!--Estilo para alterar cor do fundo-->
    <style name="containerColor">
        <item name="android:background">@color/colorbgNight</item>
    </style>
</resources>